import { StyleSheet } from "react-native";

export const DataBaseStyles = StyleSheet.create({
    Delicious : {
        fontSize:70,
        fontWeight: 'bold',
        textAlign: 'center',
    },
})